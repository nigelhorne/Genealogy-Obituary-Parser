---
# Pass through App::Test::Generator/fuzz-harness-generator

module: Genealogy::Obituary::Parser
function: parse_obituary

config:
  test_nuls: off
  test_non_ascii: off

input:
  text:
    type: string
    min: 1
    max: 10000
  geocoder:
    type: object
    can: geocode
    optional: 1

output:
  type: hashref
  min: 1
  max: 10
  optional: 1
